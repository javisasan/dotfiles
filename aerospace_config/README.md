## Aerospace configuration (for MacOs)

- `aerospace.toml` should be placed in home directory.
- `kitty.conf` should be placed in `~/.config/kitty/`

(Note: check if should have a dot in the beginning)
